{
  "name": "oscripts",
  "version": "1.0.0",
  "description": "My multiple purposes scripts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "exceljs": "^0.2.3",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "gulp-shell": "^0.5.1"
  }
}
